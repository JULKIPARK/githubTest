<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pwc.pwcesg.frontoffice.about.mapper.AboutMapper">

	<!-- About Us 목록 조회 -->
	<select id="selectAboutList" parameterType="camelcasemap" resultType="camelcasemap">
		SELECT SYSDATE FROM DUAL
	</select>
	
</mapper>